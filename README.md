# Matsuk Aliaksei 
> The build works for gulp version 4.0. 

#### To get started

1. ```clone this repo```
2. ```cd path/to/liveHTML```
3. ```npm install gulpjs/gulp-cli -g```
4. ```npm install```
6. ```run gulp``` 

#### or open index.html from build folder